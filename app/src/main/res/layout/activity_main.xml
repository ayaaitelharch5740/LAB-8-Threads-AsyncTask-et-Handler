<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center_horizontal">

    <TextView
        android:id="@+id/labelInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="État : en attente"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingBottom="16dp"/>

    <ProgressBar
        android:id="@+id/barreProgression"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="0"
        android:visibility="invisible"/>

    <ImageView
        android:id="@+id/vueImage"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_marginTop="20dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="Aperçu image"/>

    <Button
        android:id="@+id/btnDemarrerThread"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Lancer Thread (image)"
        android:layout_marginTop="20dp"/>

    <Button
        android:id="@+id/btnLancerTache"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Démarrer tâche lourde"
        android:layout_marginTop="10dp"/>

    <Button
        android:id="@+id/btnMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tester réactivité UI"
        android:layout_marginTop="10dp"/>

</LinearLayout>